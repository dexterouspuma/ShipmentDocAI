"""Shared domain models and enums for Shipment Document AI.

Imported by both the API service and the extraction workers so that field names
and types stay consistent across Document Intelligence mapping, SQL persistence,
the review API, and cover-sheet generation.
"""
